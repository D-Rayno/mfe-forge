# Agent OS

`AGENTS.md` is the repository contract.

- `rules/` contains invariants that apply across tasks.
- `skills/` contains task procedures.
- `../docs/ai/` contains persistent architecture knowledge and ADRs.

A skill is complete only when it defines when to use it, required context, procedure, verification, and expected artifacts.
