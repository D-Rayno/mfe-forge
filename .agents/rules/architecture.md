# Architecture rules

- Keep package boundaries explicit.
- Put reusable CLI behavior in `packages/cli/src/utils`.
- Treat the manifest as generated project topology and `mfeforge.config.*` as policy.
