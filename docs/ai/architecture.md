# AI engineering architecture

The CLI uses `mfeforge.config.*` for project policy and `mfe-forge.manifest.json` for generated topology. Discovery may reconcile the manifest with source files so stale metadata is diagnosable.
